<!DOCTYPE html>
<html>
    <head>
        <title>Tenant Management System</title>
        <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
        <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
        <link rel="stylesheet" href="/stylesheets/main.css">
    </head>
    <body>
        <nav class="navbar navbar-default">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a class="navbar-brand" style="font-weight: bolder;font-family:sans-serif;font-size: x-large;" href="/">Tenant Management System</a>
                </div>
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav navbar-right">
                        <% if(!currentuser){ %>
                            <li><a style="font-size: larger;color: rebeccapurple;" href="/login">Login</a></li>
                            <li><a style="font-size: larger;color: rebeccapurple;" href="/register">Sign Up</a></li>
                        <%}else{ %>
                            <li><a style="font-size: larger;color: rebeccapurple" href="#">Signed In As <%=currentuser.username%></a></li>
                            <li><a style="font-size: larger;color: rebeccapurple;" href="/logout">Logout</a></li>
                        </ul>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>
        <div class="container"> 
            <% if(error && error.length>0){ %>
                <div class="container-fluid">
                    <div class="alert alert-success alert-dismissible" role="alert">
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span
                                aria-hidden="true">&times;</span></button>
                        <%= error %>
                    </div>
                </div>
            <% } %>
            <% if(success && success.length>0){ %>
                <div class="container-fluid">
                    <div class="alert alert-success alert-dismissible" role="alert">
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span
                                aria-hidden="true">&times;</span></button>
                        <%= success %>
                    </div>
                </div>
            <% } %>  
        </div>
